-- بيضيف الأعمدة اللازمة عشان نشتغل على "نفس ملف الإكسيل الأصلي" اللي رفعه
-- المستخدم بدل ما نبني ملف جديد من الصفر وقت التحميل.
--
-- الفكرة: بنخزن الملف الأصلي كامل (بصيغة base64) في original_file_b64.
-- كل خطوة معالجة (process-step) بتفتح الملف ده، تعدّل خلية واحدة بس (نتيجة
-- الـ AI لعمود الهدف)، وتحفظه تاني في نفس العمود — يعني أي شيت تاني، تنسيق
-- الأعمدة، الدمج، المعادلات... أي حاجة موجودة في الملف الأصلي وميتلمسش
-- بتفضل زي ما هي، لأننا مش بنبني الملف من JSON من الأول.

alter table jobs add column if not exists original_file_b64 text;
alter table jobs add column if not exists original_filename text;
alter table jobs add column if not exists row_count int;

-- ملاحظة: عمودي rows و headers القدام (من 0001) مبقوش بيتكتب فيهم بيانات
-- كبيرة تاني بعد التعديل ده (rows بقى غير مستخدم خالص)، لكن سيبناهم في
-- الجدول عشان مفيش داعي نكسر أي بيانات قديمة — مش هيأثروا على حاجة.
